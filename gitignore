# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# Logs & temp
*.log
*.tmp
.cache/

# Secrets / local env — never commit these
.env
.env.*
*.local
secrets.json

# Project-local notes — private build handoff, not for publishing
memory.md

# Python (only relevant if the backlog ever adds Python tooling)
__pycache__/
*.py[cod]
.venv/
venv/